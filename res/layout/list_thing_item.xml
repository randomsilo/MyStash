<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content" >

    	<TextView
    	    android:id="@+id/thing_id"
    	    android:layout_width="wrap_content"
    	    android:layout_height="wrap_content"
    	    android:layout_weight="1"
    	    android:padding="10dp"
    	    android:paddingRight="@dimen/spacer"
    	    android:text="@string/empty_id"
    	    android:textColor="@color/common_text"
    	    android:textSize="@dimen/label_text_size"
    	    android:visibility="gone" >

	    </TextView>

        <TextView
            android:id="@+id/thing_tag"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="2"
            android:clickable="true"
            android:padding="10dp"
            android:text="@string/empty_tag"
            android:textColor="@color/theme_lite"
            android:textSize="@dimen/list_text_size" >

	    </TextView>

	    <ImageButton
	        android:id="@+id/removeThingBtn"
	        android:layout_width="wrap_content"
	        android:layout_height="wrap_content"
	        android:layout_gravity="right"
	        android:layout_weight="1"
	        android:background="@color/tinted"
	        android:contentDescription="@string/button_remove_thing"
	        android:padding="10dp"
	        android:src="@drawable/button_remove_small" />
	    
</LinearLayout> 